{
  "name": "ukulele-tuner",
  "version": "0.0.1",
  "description": "Online ukulele tuner",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "next",
    "start": "next start",
    "build": "next build",
    "static:build": "next build && next export && touch out/.nojekyll",
    "static:clean": "rm -rf out",
    "static:help": "next export -h",
    "static:deploy": "yarn static:build && git subtree push --prefix out origin gh-pages && yarn static:clean"
  },
  "dependencies": {
    "audio-context": "^1.0.1",
    "get-user-media-promise": "^1.1.1",
    "next": "^3.0.3",
    "pitchfinder": "^2.0.4",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "babel-plugin-transform-decorators-legacy": "^1.3.4"
  }
}
